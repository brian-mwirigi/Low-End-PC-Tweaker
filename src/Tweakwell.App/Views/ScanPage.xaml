<Page
    x:Class="Tweakwell.App.Views.ScanPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Background="{StaticResource TwBgBrush}">
    <ScrollViewer HorizontalScrollBarVisibility="Disabled">
        <Grid>
        <StackPanel Padding="28,20,28,32" Spacing="22" MaxWidth="980" HorizontalAlignment="Center">
            <Grid>
                <StackPanel Spacing="4">
                    <TextBlock Text="This machine" Style="{StaticResource PageTitle}" />
                    <TextBlock
                        x:Name="LeadText"
                        Style="{StaticResource PageLead}"
                        Text="Read-only. Tweakwell has not changed the registry, the power plan, or any files." />
                </StackPanel>
                <Button
                    x:Name="RefreshButton"
                    Content="Scan again"
                    Style="{StaticResource GhostButton}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Top"
                    Click="Refresh_Click" />
            </Grid>

            <StackPanel x:Name="FindingsHost" Spacing="10" />

            <Grid x:Name="SpecGrid" ColumnSpacing="10" RowSpacing="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
            </Grid>

            <StackPanel Spacing="8">
                <Grid>
                    <TextBlock Text="Startup" Style="{StaticResource PageTitle}" FontSize="18" />
                    <TextBlock
                        x:Name="StartupCount"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Style="{StaticResource PageLead}" />
                </Grid>
                <TextBlock
                    Style="{StaticResource PageLead}"
                    Text="All-users entries are listed so you can see them. Only this-user items can be disabled from Tweaks." />
                <Border
                    Background="{StaticResource TwSurfaceBrush}"
                    BorderBrush="{StaticResource TwLineBrush}"
                    BorderThickness="1"
                    CornerRadius="10"
                    Padding="12,4">
                    <StackPanel x:Name="StartupHost" />
                </Border>
            </StackPanel>
        </StackPanel>
        </Grid>
    </ScrollViewer>
</Page>
